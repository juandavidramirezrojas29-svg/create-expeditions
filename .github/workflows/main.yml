name: Build Create Expeditions Mod

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'
      - name: Build with Gradle
        run: gradle build
      - name: Upload JAR as artifact
        uses: actions/upload-artifact@v4
        with:
          name: create-expeditions-jar
          path: build/libs/create-expeditions-0.1.0.jar
